# Paperclip + UAW Governance Integration

## Project State

Phase: structural
Objective: Orchestrate AI agent workflows across multiple ventures using Paperclip + UAW v1 contract + TodoFoco Governance Framework.
Active Specs: design/specs/2026-04-02-paperclip-uaw-governance-design-Phase-1.md
Completion: Config done. Testing revealed 6 operational issues. Workarounds applied. Pipeline flexibility proposal drafted.

---

## Resume Point

Updated: 2026-04-03

### Active Task

Testing and debugging the first real agent runs after company import.

### Stopped At

Discovered and documented 6 issues during testing:

1. **Issue 1 (High):** Agents can't read env vars — Claude Code sandbox blocks $VAR expansion, printenv, /proc/self/environ. SpecWriter burned ~3M tokens discovering workaround. Fix: dangerouslySkipPermissions: true + X-Local-Agent-Id header.
2. **Issue 2 (Medium):** Checkout conflict — heartbeat pre-sets executionRunId, agent's /checkout call gets 409. Tokens wasted on debugging. Fix: note in instructions that pre-set lock = implicit checkout.
3. **Issue 3 (Medium):** Company import doesn't attach workspaces. Manual API/UI setup required.
4. **Issue 4 (Medium):** Company deletion returns 500 — missing table in cascade delete sequence in companyService.remove(). Fix: DB reset or reimport with --collision skip.
5. **Issue 5 (Low):** Codex fails in non-git directories. Expected but confusing. Fix: git init all project dirs.
6. **Issue 6 (High):** Superpowers plugin injects ~200k tokens per heartbeat via SessionStart hook. Major cost impact. Fix: patched hook to skip when PAPERCLIP_RUN_ID is set.

Also drafted pipeline flexibility proposal (4 modes: supervised, automated, spec_provided, quick).

### Open Decision

Whether to report Issues 1, 4, 6 upstream to paperclipai/paperclip.

### Head State

- dangerouslySkipPermissions: true now set on all claude_local agents (required workaround)
- Agent runs are functional but expensive due to Issue 6 (superpowers token overhead)
- Workspace attachment is manual (Issue 3) — needs to be done after every fresh import
- Pipeline flexibility proposal is draft — not yet implemented
- All 6 issues documented in design/issues/paperclip-issues-log.md

### Next Action

Decide on upstream issue reporting. Continue testing agent workflows with governance framework. Consider v2026.403.0 upgrade.
